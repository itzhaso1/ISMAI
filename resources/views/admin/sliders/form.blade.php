@extends('layouts.admin')
@section('title', $slider->exists ? 'Edit Slide' : 'New Slide')
@section('page_title', $slider->exists ? 'Edit Slide' : 'New Slide')
@section('content')
<form class="admin-panel admin-form" method="POST" enctype="multipart/form-data" action="{{ $slider->exists ? route('admin.sliders.update', $slider) : route('admin.sliders.store') }}">@csrf @if($slider->exists) @method('PUT') @endif
<div class="form-grid"><label>Arabic title<input name="title_ar" value="{{ old('title_ar', $slider->title_ar) }}" required></label><label>English title<input name="title_en" value="{{ old('title_en', $slider->title_en) }}" required></label><label>Arabic subtitle<textarea name="subtitle_ar">{{ old('subtitle_ar', $slider->subtitle_ar) }}</textarea></label><label>English subtitle<textarea name="subtitle_en">{{ old('subtitle_en', $slider->subtitle_en) }}</textarea></label><label>Arabic button<input name="button_text_ar" value="{{ old('button_text_ar', $slider->button_text_ar) }}"></label><label>English button<input name="button_text_en" value="{{ old('button_text_en', $slider->button_text_en) }}"></label><label>Button URL<input name="button_url" value="{{ old('button_url', $slider->button_url) }}"></label><label>Overlay color<input name="overlay_color" value="{{ old('overlay_color', $slider->overlay_color ?: 'rgba(16, 16, 20, .68)') }}"></label><label>Sort order<input type="number" name="sort_order" value="{{ old('sort_order', $slider->sort_order ?? 0) }}"></label><label>Image<input type="file" name="image" accept="image/*"></label></div><div class="check-grid"><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $slider->is_active ?? true))> Active</label></div><button class="btn-primary" type="submit">Save Slide</button></form>
@endsection
