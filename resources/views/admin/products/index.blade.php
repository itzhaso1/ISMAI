@extends('layouts.admin')
@section('title', 'Products')
@section('page_title', 'Products')
@section('content')
<div class="admin-toolbar"><form class="toolbar-form"><input name="q" value="{{ request('q') }}" placeholder="Search products"><select name="status"><option value="">All statuses</option><option value="active" @selected(request('status') === 'active')>Active</option><option value="inactive" @selected(request('status') === 'inactive')>Inactive</option></select><button type="submit">Filter</button></form><a class="btn-primary" href="{{ route('admin.products.create') }}">New Product</a></div>
<section class="admin-panel"><div class="table-wrap"><table class="admin-table"><thead><tr><th>Product</th><th>Category</th><th>Brand</th><th>Price</th><th>Images</th><th>Status</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr><td><strong>{{ $product->name_en }}</strong><small>{{ $product->sku }}</small></td><td>{{ $product->category?->name_en }}</td><td>{{ $product->brand?->name }}</td><td>{{ number_format((float) $product->price, 2) }}</td><td>{{ $product->images_count }}</td><td>{{ $product->is_active ? 'Active' : 'Inactive' }}</td><td class="row-actions"><a href="{{ route('products.show', $product) }}" target="_blank">View</a><a href="{{ route('admin.products.edit', $product) }}">Edit</a><form method="POST" action="{{ route('admin.products.destroy', $product) }}">@csrf @method('DELETE')<button onclick="return confirm('Delete product?')">Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $products->links() }}</section>
@endsection
